import { SettingsState } from 'src/types';
import { Action } from 'src/actions';

export default function settings(
  state: SettingsState = {},
  _action: Action
): SettingsState {
  return state;
}
